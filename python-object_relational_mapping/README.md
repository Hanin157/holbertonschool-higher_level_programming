# Python - Object Relational Mapping

This project uses Python to interact with a MySQL database.

It covers using the MySQLdb module to connect to MySQL and execute queries,
and later introduces SQLAlchemy as an ORM to map Python objects to database tables.
